const apiRoutes = {
  gists: "/api/gists"
};

const clientRoutes = {
  root: "/",
  signIn: "/sign-in",
  signUp: "/sign-up",
  gists: "/gists",
  createGist: "/gists/create"
};

export { apiRoutes, clientRoutes };
